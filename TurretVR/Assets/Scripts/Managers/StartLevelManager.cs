using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartLevelManager : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        //var player = Instantiate(playerPrefab, playerStartPosition, Quaternion.identity);
        //player.SetActive(true);
        //var camera = player.GetComponentInChildren<Camera>();
       // camera.transform.parent = null;
        //spaceCamera.parentCamera = camera;
        //spaceCamera.relativeFocusObject = camera.transform;
        //GameManager.Instance.GameCamera = camera;

        
        

        //GameManager.Instance.Player = player;
        
    }
}
